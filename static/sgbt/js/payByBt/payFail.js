require(['jquery', 'util', 'Const', 'bvLayout'], function($, util, Const) {

    var applSeq = util.gup('applSeq');

    var vm = util.bind({
        container: 'payFail',
        data: {
        },
        methods: {
            payFailFn: function(){
                //TODO
            }
        }
    });
});