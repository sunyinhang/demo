require(['avalon', 'jquery', 'bootstrap', 'util', 'Const'], function(avalon, $, bootstrap, util, Const) {
    util.init();
    // 初始化实体类定义
    //util.initEntities();
    // 字典
    // util.initDicts();
});